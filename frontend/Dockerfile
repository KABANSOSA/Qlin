FROM node:18-alpine

WORKDIR /app

# Install dependencies will be done via volume mount
# This speeds up development - dependencies installed locally

# Expose port
EXPOSE 3000

# Default command
CMD ["npm", "run", "dev"]
